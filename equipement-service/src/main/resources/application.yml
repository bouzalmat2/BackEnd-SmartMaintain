server:
  port: 8081

spring:
  application:
    name: equipement-service
  datasource:
    url: jdbc:postgresql://localhost:5432/equipementdb
    driver-class-name: org.postgresql.Driver
    username: karima
    password: karima123
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
