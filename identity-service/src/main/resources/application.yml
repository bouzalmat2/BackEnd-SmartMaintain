server:
  port: 8085

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/smartmaintain_db
    username: karima
    password: karima123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update  # Rej3iha update bach i-creyi lik les tables
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect